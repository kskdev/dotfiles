# dotfiles

各種開発環境やアプリの設定ファイルを管理するレポジトリ．
特定の環境に依存せず，`git clone` だけで再現性のある環境構築の補助を目的としている．

## 構成

主要なディレクトリ構成は以下の通り．

- **Git/**: Git の共通設定，エイリアス，グローバルな ignore 設定
- **Shell/**: zsh/bash の設定ファイル，便利なコマンド群
- **Vim/**: Vim/GVim の設定 (ddc.vim による補完対応)
- **Tmux/**: tmux の設定
- **VScodeLike/**: VSCode や Antigravity 用の設定と拡張機能リスト
- **Ubuntu/**: Ubuntu 環境の初期セットアップ用スクリプト

## セットアップ

### 一括インストール (Ubuntu 向け)

```bash
sh init.sh
```

### 個別インストール

各ディレクトリにある `make_link.sh` や `install.sh` を使用．詳細は各ディレクトリの README を参照．

## 運用ポリシー

- **環境対応**: macOS，Ubuntu，Windows などの実行環境の変動や，git clone しか許可されていない環境での利用を想定．
- **セキュリティ**: 機密情報はレポジトリの管理対象外とし，それらの情報が含まれているファイルはコミットを禁止．
- **ツールの導入**: 管理コスト削減のため，既知であるツール以外の新しいツールやアプリの導入は非推奨．

## Log

#### 2026/01/01

- Git 設定の整理とセキュリティ強化
  - `.gitconfig` から個人情報を分離し `~/.gitconfig.local` を利用する構成へ移行
  - 秘密情報の混入を防ぐため `.gitignore_global` を強化
- インストーラおよび各種初期化スクリプトのバグ修正
- Antigravity (VSCode fork) 用の設定・インストーラを追加
- レポジトリ運用ポリシーの明文化および README への反映

#### 2025/06/03

- Vim/GVim のメンテナンス

#### 2024/01/08

- Vim の補完プラグインを `ddc.vim` (+ `pum.vim`) へ刷新
- VSCode 設定ファイル (JSON) を追加

#### 2021/04/10

- 一括インストール用スクリプト `init.sh` を追加
